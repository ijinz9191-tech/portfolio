# CareerOps 공개 포트폴리오

외부 패키지·추적기·원격 폰트가 없는 한국어 반응형 정적 사이트입니다. 이 디렉터리는 설치 가능한 소스 자산이며, 요구사항의 실제 사이트 Source of Truth는 `C:/PRJ/apps/portfolio`입니다. 설치할 때 기존 파일과 저장소 상태를 보존합니다.

## 로컬 확인

Node.js 20 이상에서 의존성 설치 없이 실행합니다.

```powershell
npm test
npm run build
npm start
```

로컬 주소는 `http://127.0.0.1:4173`입니다. 이는 외부 공개 URL이 아닙니다. 서버는 `dist`의 네 개 허용 자산만 제공하고 나머지 경로는 차단합니다. `dist`는 재생성 가능한 빌드 결과이며 Git에 포함하지 않습니다.

## 공개 데이터 갱신

`public.data.json`은 경력 원본에서 분리된 **공개 전용 투영 데이터**입니다. CareerOps는 기능 검증·릴리스·실험·공개 이력 변경 이벤트에서 허용된 요약을 생성한 후 다음 명령을 호출할 수 있습니다.

```powershell
npm run update:public -- path/to/sanitized-public.json
npm test
npm run build
```

허용 필드와 검사는 `scripts/public-contract.mjs`에 정의합니다. 알 수 없는 필드, 대표적 비밀·연락처 패턴, 로컬 경로, 자격증명을 포함한 링크와 자리표시자 도메인을 거부합니다. 이 검사는 모든 기밀을 탐지하는 DLP 제품이 아닙니다. 원본 자료와 비공개 상태를 이 폴더에 넣지 말고 공개 허용 범위 확인을 먼저 수행해야 합니다. 연락처 공개는 추후 별도 계약 변경이 필요한 범위입니다.

`implemented`는 소스 구현, `planned`는 계획, `unverified`는 검증 근거가 연결되지 않은 상태입니다. 지표는 분모·범위·확인 시각·공개 근거 URL이 있어야 합니다. URL 형식 검사는 실제 접근 가능성과 주장의 진위를 증명하지 않습니다. 게시 전 외부 링크·배포 commit·표시 내용 일치를 실제로 확인해야 합니다.

## 배포와 실제 경력

`dist`를 허용된 정적 호스팅에 게시합니다. 이 자산은 배포 계정·원격 CI 연결을 가정하지 않습니다. 실제 외부 URL, CI 결과, Git 원격 commit은 해당 실행이 끝난 뒤 CareerOps의 자산 장부에 등록합니다. 배포 실패 시 이전 검증된 배포를 유지합니다.

기본 데이터는 실제 경력·학력·자격증·성과·지원 접수 건수를 넣지 않습니다. 사용자 역할은 제공된 목표·요구사항, AI 역할은 이 사이트의 코드 작성으로 구분합니다. 허위 경력이나 운영 성과를 추정하여 게시하지 않습니다.

검사는 데이터 계약, 민감 입력·위험 링크 거부, 내부 앵커·자산 참조, 정상 렌더링과 데이터 로딩 오류 경로를 다룹니다. 실제 브라우저 모바일·데스크톱 확인 및 비로그인 외부 접근 검증은 배포 단계에서 별도로 수행합니다.
